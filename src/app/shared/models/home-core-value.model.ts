export class HomeCoreValue {
  title: string;
  imageFileName: string;
  altText: string;

  constructor(title: string, imageFileName: string, altText: string) {
    this.title = title;
    this.imageFileName = imageFileName;
    this.altText = altText;
  }
}
