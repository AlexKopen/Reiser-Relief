export class FounderRow {
  imageFileName: string;
  content: string;

  constructor(imageFileName: string, content: string) {
    this.imageFileName = imageFileName;
    this.content = content;
  }
}
