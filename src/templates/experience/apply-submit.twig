{{ include('experience/experience-header.twig') }}

<section>
    <div id="page-info">
        <div class="padding-hold">
            <h1 id="page-info-heading">
                {% if FormSuccess %}
                    Thank you for applying.  A team member will be in touch with you shortly.
                {% else %}
                    There was an error submitting your application.  Please go back and try again.
                {% endif %}
            </h1>
        </div>
    </div>
</section>

{{ include('experience/experience-footer.twig') }}
