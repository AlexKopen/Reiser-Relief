{{ include('about/about-header.twig') }}

<div class="container main-page-content">
    <div id="sub-page-navigation">
        <nav class="padding-hold">
            <ul class="pure-menu-list">
                <li class="pure-menu-item"{{ Active == 'Core Values' ? ' id="active"' : '' }}>
                    <a href="/about/core-values">Core Values</a>
                </li>
                <li class="pure-menu-item"{{ Active == 'Board Members' ? ' id="active"' : '' }}>
                    <a href="/about/board-members">Board Members</a>
                </li>
                <li class="pure-menu-item"{{ Active == 'Our Founder' ? ' id="active"' : '' }}>
                    <a href="/about/our-founder">Our Founder</a>
                </li>
            </ul>
        </nav>
    </div>
    <div id="content">
        <div class="padding-hold">
            <h2 class="page-section-header">
                {{ Active }}
            </h2>
            {% if Active=="Core Values" %}
                {{ include('about/core-values.twig') }}
            {% elseif Active=="Board Members" %}
                {{ include('about/board-members.twig') }}
            {% elseif Active=="Our Founder" %}
                {{ include('about/our-founder.twig') }}
            {% endif %}
        </div>
    </div>
</div>

{{ include('about/about-footer.twig') }}
