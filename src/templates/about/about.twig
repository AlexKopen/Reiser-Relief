{{ include('about/about-header.twig') }}

<div class="container main-page-content">
    <div id="sub-page-navigation">
        <nav>
            <ul class="pure-menu-list">
                <li class="pure-menu-item"{{ Active == 'Our Core Values' ? ' id="active"' : '' }}>
                    <a href="/about/our-core-values">Core Values</a>
                </li>
                <li class="pure-menu-item"{{ Active == 'Our Board' ? ' id="active"' : '' }}>
                    <a href="/about/our-board">The Board</a>
                </li>
                <li class="pure-menu-item"{{ Active == 'Our Founder' ? ' id="active"' : '' }}>
                    <a href="/about/our-founder">Our Founder</a>
                </li>
            </ul>
        </nav>
    </div>
    <div id="content">
        <div class="padding-hold">
            <h2 class="page-section-header">
                {{ Active }}
            </h2>
            {% if Active=="Our Core Values" %}
                {{ include('about/our-core-values.twig') }}
            {% elseif Active=="Our Board" %}
                {{ include('about/our-board.twig') }}
            {% elseif Active=="Our Founder" %}
                {{ include('about/our-founder.twig') }}
            {% endif %}
        </div>
    </div>
</div>

{{ include('about/about-footer.twig') }}
